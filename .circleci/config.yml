version: 2.1
jobs:
  build:
    docker:
      - image: debian:stretch
      
    steps:
      - checkout
      
      - run:
          name: Greeting
          command: echo Hello World!
          
      - run:
          name: Print Current Time
          command: date
          
