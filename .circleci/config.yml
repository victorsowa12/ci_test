version: 2.1
jobs:
  build:
    docker:
      - image: continuumio/miniconda3
      
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            conda create -n xMoonJelly_fw
            source activate xMoonJelly_fw
            conda env update -f environment.yml
      - run: python conda_python_test.py      

workflows:
  version: 2.1
  build_and_test:
    jobs:
      - build


          
